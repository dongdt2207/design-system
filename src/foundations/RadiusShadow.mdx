import { Meta } from '@storybook/addon-docs/blocks';
import { RadiusScale, ShadowScale } from './TokenBlocks';

<Meta title="Foundations/Radius & Shadow" />

# Radius & Shadow
## Radius
`sm` 6 checkbox, tag · `md` 8 button, input · `lg` 12 card, modal, dropdown · `xl` 16 large card (rare) · `full` badge, avatar.
<RadiusScale steps={['sm', 'md', 'lg', 'xl', 'full']} />

## Shadow — this system has none
Depth comes from hairlines: `--shadow-sm/md/lg` are 1px borders at three weights, not drop shadows. Cards lift through white-on-cream.
<ShadowScale steps={['sm', 'md', 'lg']} />
